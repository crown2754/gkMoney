{
  "spec_markdown": "# 功能規格：銀行帳戶管理（Laravel 11 + PostgreSQL + TALL）\n\n## 目標\n- 讓使用者新增、編輯、刪除自己的多幣別銀行帳戶。\n- 帳戶餘額以原始幣別儲存，另存 `balance_twd` 為當日匯率換算後的台幣金額。\n- 首頁儀表板顯示當日總資產（台幣）＝所有活躍帳戶 `balance_twd` 之和。\n- 支援負餘額（透支），將減少總資產。\n- 提供管理員手動編輯匯率介面，編輯後自動重新計算該幣別所有帳戶的 `balance_twd`。\n- 每日自動從臺灣銀行匯率 API 抓取匯率，並產生每位使用者的總資產快照供日後圖表使用。\n\n## 限制與約束\n- 技術棧：Laravel 11 + PostgreSQL + TALL (Tailwind + Alpine.js + Livewire + Laravel)。\n- `users.id` 為 `bigint`，所有外鍵必須使用 `foreignId()` 對應此型別。\n- 匯率精度：`exchange_rates.rate_to_twd` 為 `decimal(15,6)`；餘額與台幣等值為 `decimal(15,2)`。\n- 匯率快取：今日匯率快取 1 小時，過期自動重新抓取。\n- 排程安全：匯率更新與資產快照皆設定 `withoutOverlapping()`。\n- 權限：只有帳戶擁有者可操作自己的帳戶；匯率編輯僅限管理員。\n- 金額顯示格式：千分位逗號＋兩位小數（例如 `1,234,567.89`）。\n- 測試覆蓋率目標 ≥ 80%。\n\n## 資料庫設計\n