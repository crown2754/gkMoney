{
  "spec_markdown": "# 功能規格：修正 migration 中 user_id 型別不匹配\n\n## 問題描述\n- 執行 `php artisan migrate` 時發生錯誤：\n  `SQLSTATE[42804]: user_id uuid 與 users.id bigint 不符`\n- 原因：`database/migrations/2026_05_19_112753_create_expenses_table.php` 中的 `user_id` 欄位被定義為 `uuid`，而 `users.id` 是 `bigint`（Laravel 11 預設的 `$table->id()`）。\n\n## 目標\n- 修正 migration，使 `user_id` 使用 `foreignId()`（對應 `bigint unsigned`），並確保外鍵正確指向已存在的 `users` 表。\n- 必要時調整相關 Model 或 Livewire 元件，以保持關係正確。\n- 讓 `php artisan migrate` 能夠成功執行，且不破壞現有功能。\n\n## 變更說明\n1. **Migration 檔案**\n   - 檔案路徑：`database/migrations/2026_05_19_112753_create_expenses_table.php`\n   - 將 `user_id` 欄位從 `->uuid('user_id')` 改為 `->foreignId('user_id')->constrained()->onDelete('cascade')`（或視業務需求選擇 `onDelete('set null')`）。\n   - 確保 migration 中沒有參照尚未建立的其他表格。\n\n2. **Expense Model（如有需要）**\n   - 檢查 `App\Models\Expense.php` 是否已有 `public function user()` 關係，應該是 `return $this->belongsTo(User::class);`。\n   - 若模型中有自訂 `$keyType` 或 `$incrementing` 設定導致型別不一致，請移除或正確設定為 `$keyType = 'int'; $incrementing = true;`（預設即可）。\n\n3. **Livewire 元件（若有直接操作 user_id）**\n   - 確認任何表單或資料綁定中使用的 `user_id` 是整數 ID，而非 UUID。\n   - 無需變更元件本身，只需確保後端傳入的值為 `auth()->id()` 或其他整數 ID。\n\n4. **Migration 執行驗證**\n   - 在本地開發環境執行 `php artisan migrate:fresh --seed`，確認無錯誤。\n   - 檢查 `expenses` 表結構：`user_id` 欄位型別為 `bigint unsigned`，並有外鍵約束指向 `users.id`。\n\n## 預期結果\n- `php artisan migrate` 成功完成。\n- 建立新 expense 時，`user_id` 正確儲存為目前登入使用者的整數 ID，且不觸發外鍵錯誤。\n- 現有單元測試與功能測試仍能通過。\n"
}